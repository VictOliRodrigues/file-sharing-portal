<% content_for :title, "Dashboard" %>

<div class="card p-8 text-center">
  <h2 class="text-lg font-semibold text-slate-900">Welcome, <%= current_user.name %>.</h2>
  <p class="mt-2 text-sm text-slate-500">File management arrives in the next release.</p>
</div>
